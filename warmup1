#Jack Robey
#9/6/17
#warmUp1.py - warmup program

base = float(input('Enter the length of the base: '))
height = float(input('Enter the length of the height: '))
area = 1/2*base*height
print('The area is', area)
